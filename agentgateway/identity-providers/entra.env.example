# Not yet implemented — tracked as a follow-up issue once Cognito (see
# cognito.env.example) is proven out. Same seam as okta.env.example: only
# this file plus a role-claim source in Entra ID's tokens (via an app
# registration's optional claims / app roles) should be needed — no changes
# to config.yaml.template or the CEL policy in docker-compose.yml.
#
# Expected shape once filled in:
# OIDC_ISSUER=https://login.microsoftonline.com/<tenant-id>/v2.0
# OIDC_AUDIENCE=<application-client-id>
# OIDC_JWKS_URI=https://login.microsoftonline.com/<tenant-id>/discovery/v2.0/keys
